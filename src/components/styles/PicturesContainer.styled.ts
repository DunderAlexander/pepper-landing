import styled from "styled-components";

export const PicturesContainer = styled.div`
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  /* grid-auto-rows: minmax(100%, 5rem); */
  gap: 2rem;
`;
